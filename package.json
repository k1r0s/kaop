{
  "name": "kaop",
  "version": "2.7.6",
  "description": "simple js OOP/AOP lib helper with classes, inheritance and method Advices (method interceptors)",
  "main": "lib/index.js",
  "jest": {
    "verbose": true
  },
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "version": "browserify ./lib/index.js -o ./dist/kaop-lib.dev.js ; uglifyjs ./dist/kaop-lib.dev.js --compress --mangle --keep-fnames --output ./dist/kaop-lib.min.js",
    "preversion": "jest --coverage && cat coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
    "postversion": "git push --follow-tags",
    "report-cover": "./coverage.s"
  },
  "keywords": [
    "framework",
    "helper",
    "oop",
    "aop"
  ],
  "author": "k1r0s",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/k1r0s/kaop.git"
  },
  "homepage": "https://github.com/k1r0s/kaop",
  "bugs": {
    "url": "https://github.com/k1r0s/kaop/issues"
  },
  "license": "ISC",
  "devDependencies": {
    "browserify": "^14.1.0",
    "coveralls": "^2.12.0",
    "jest": "^19.0.2",
    "uglifyjs": "^2.4.10"
  }
}
